# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: Arrival ID
        searchable: true
    name:
        label: Student name
        searchable: true
    arrival_date:
        label: Time of arrival
    user:
        label: User
        relation: user
        select: concat(name, ' - ', email)
